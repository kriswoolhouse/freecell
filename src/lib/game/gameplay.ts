export function moveCard() {
  // TODO
  // Is the card the start of a sub-stack?
  // --> Is there a chain of cards below it?
  // --> Is there anywhere to move the sub-stack to?
  // --> Are there enough free cells and open cells to move the sub-stack?
  // if (cardIsStartOfSubStack()) {
  //   const availableSpotForSubStack = getSpotForSubStack();
  //   if (availableSpotForSubStack && canMoveSubStack()) {
  //     moveSubStack();
  //     return;
  //   } else {
  //     shakeSubStack();
  //     return;
  //   }
  // }
  // Is the card movable at all?
  // --> Can it go on a foundation?
  // --> Can it be placed at the bottom of a sub-stack?
  // --> Are there any open cells?
  // --> Are there any available free cells?
  // if (canMoveToFoundation()) {
  //   // Move card to foundation
  // }
  //
  // if (canMoveToBottomOfSubStack()) {
  //   // Move card to bottom of sub-stack
  // }
  //
  // if (canMoveToOpenCell()) {
  //   // Move card to open cell
  // }
  //
  // if (canMoveToFreeCell()) {
  //   // Move card to free cell
  // }
  //
  // shakeCard();
}

// TODO
// How can I update the state of tableau, freecells & foundations
